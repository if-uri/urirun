"""Local compatibility package for the extracted URI router.

The router is declared as an external distribution, but development checkouts
must still run when that wheel is not available from the configured index.
"""

